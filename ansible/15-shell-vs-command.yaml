- name: ansible from command vs shell module
  hosts: local
  connection: local
  tasks:
  - name: command module test
    ansible.builtin.command: cat etc/passwd | grep root
    register: user_output
