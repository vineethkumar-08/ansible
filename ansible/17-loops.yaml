- name: installing the packages
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: install packages
    ansible.debug.package:
      name:
      state: present
    loop:
    - mysql
    - nginx
    - zip